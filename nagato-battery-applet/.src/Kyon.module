' Gambas module file

Private $trayIcon As NagatoTrayIcon

Public Sub Main()
    
    If Desktop.HasSystemTray Then
        MikuruPowerManager.Activate()
        $trayIcon = New NagatoTrayIcon
    Else
        Message.Error(("YUKI.N > No systray."), ("OK"))
    End If
    
End
