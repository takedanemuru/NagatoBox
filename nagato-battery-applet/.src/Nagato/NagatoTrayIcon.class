' Gambas class file

Private $trayIcon As TrayIcon
Private $timer As New Timer As "Timer"

Public Sub _new()
    
    $trayIcon = New TrayIcon As "TrayIcon"
    
    With $trayIcon
        .Icon = MikuruIcon["battery"]
        .Show()
    End With
    
    $timer.Enabled = True
    
End

Public Sub Timer_Timer()
    
    $trayIcon.Tooltip = "foo"
    
End
