' Gambas class file

Inherits NagatoObject

Private $label As Label
Private $xmlElement As XmlElement

Public Sub Refresh(argText As String)
    
    $label.Text = argText
    
End

Public Sub _new(argContainer As Container)
    
    $label = New Label(argContainer)

    With $label
        .Text = "READY ?"
        .Expand = True
    End With
    
End
