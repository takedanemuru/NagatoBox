' Gambas class file

Create Static

Property ObservationInterval As Integer ' as millisecond

Private $settings As New Settings

Private Function ObservationInterval_Read() As Integer

    Return $settings["observation/interval", 1000]

End

Private Sub ObservationInterval_Write(Value As Integer)

    $settings["observation/interval"] = Value
    $settings.Save()

End
