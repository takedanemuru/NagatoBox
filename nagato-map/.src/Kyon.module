' Gambas module file

Public Sub Main()
    
    If Desktop.NetworkAvailable Then
        HaruhiMain.Show()
    Else
        Message.Error(("YUKI.N > I couldn't connect to network..."), ("OK"))
    End If
    
End
