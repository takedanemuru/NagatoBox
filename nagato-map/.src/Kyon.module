' Gambas module file

Public Sub Main()
    
    If Desktop.NetworkAvailable Then
        HaruhiMain.Show()
        NagatoSettings.LoadFormSettings(HaruhiMain)
    Else
        Message.Warning(("Network Unavailable"), ("OK"))
    End If
    
End
