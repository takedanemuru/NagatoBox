' Gambas class file

Static $prevTime As Float = 0

Static Public Function IsEnoughInterval(argInterval As Float) As Boolean
    
    Dim result As Boolean = False
    
    If Timer - $prevTime > argInterval Then result = True
    $prevTime = Timer
    
    Return result
    
End

