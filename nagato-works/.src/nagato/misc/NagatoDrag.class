' Gambas class file

Create Static

Property Paths As String[]

Private $paths As String[]

Private Function Paths_Read() As String[]

    If Not Object.IsValid($paths) Then
        Return [""]
    Else
        Return $paths
    Endif

End

Private Sub Paths_Write(Value As String[])

    $paths = Value

End
