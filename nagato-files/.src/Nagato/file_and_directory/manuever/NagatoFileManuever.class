' Gambas class file

Create Private
Create Static

Public Function _GetTargetDirectory(argTitle As String, argPath As String) As String
    
    Dialog.Title = argTitle
    Dialog.Path = File.Dir(argPath)
    
    Return IIf(Dialog.SelectDirectory(), "", Dialog.Path)
    
End
