' Gambas class file

Create Static

Public _$settings As New Settings

Event Changed

Public Sub _ChangeSettings(argKey As String, argValue As Variant)
    
    If Me._$settings[argKey] = argValue Then Return
    
    Me._$settings[argKey] = argValue
    Me._$settings.Save()

    Raise Changed
    
End
