' Gambas class file

Inherits NagatoChmod

Public Function _GetlabelText() As String ' virtual interface
    
    Return "9"
    
End

Public Function _GetLabelCount() As Integer
    
    Return 4
    
End

Public Function _GetBackground() As Integer
    
    Return MikuruColor.LightBlue
    
End

Public Sub _SetRadioButtons() ' virtual interface
    
    Me._SetRadioButton("-", "- : Other users cannot execute it.")
    Me._SetRadioButton("x", "x : Other users can execute it.")
    Me._SetRadioButton("T", "T : Other users cannot execute it and the 'sticky' bit is set.")
    Me._SetRadioButton("t", "t : Other users can execute it and the 'sticky' bit is set.")
    
End
