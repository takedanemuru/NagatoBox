' Gambas class file

Inherits NagatoMenuItem

Public Sub _InitializeItem() 'override
    
    Dim yukiMenu As New Menu(Me._$parentMenu) As "MenuItem"
    
    With yukiMenu
        .Text = ("Quit")
        .Picture = MikuruIcon.Get("button-power")
        .Tag = Me._$path
    End With
    
End

Public Sub MenuItem_Click()
    
    If Message.Question(("Are you sure to close nagato-files ?"), ("Cancel"), ("Close")) = 2 Then
        HaruhiMain.Close()
    End If
    
End
