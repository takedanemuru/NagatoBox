' Gambas module file

Public Sub _call()
    
    Dim yukiMessage As String = ("YUKI.N > We are going to empty trash bin...")
    
    yukiMessage &= Subst$("\n&1 items &2", NagatoTrash.Count, NagatoTrash.Size)
    
    If Message.Question(yukiMessage, ("Cancel"), ("Empty")) = 2 Then
        Shell "trash-empty"
    Endif
    
End

