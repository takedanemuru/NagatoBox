' Gambas module file

Private $lastPath As String = ""

Public Function NotChanged(argPath As String) As Boolean
    
    If $lastPath = argPath Then
        Return True
    Else
        $lastPath = argPath
        Return False
    End If
    
End

