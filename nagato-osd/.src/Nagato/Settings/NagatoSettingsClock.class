' Gambas class file

Inherits NagatoSettings

Property Type As Integer
Property ShowSeconds As Boolean

Public Enum TypeAnalog, TypeText

Private Function Type_Read() As Integer

    Return Me._$settings["clock/type", Me.TypeText]

End

Private Sub Type_Write(Value As Integer)

    Me._$settings["clock/type"] = Value
    Me._$settings.Save()

End

Private Function ShowSeconds_Read() As Boolean

    Return Me._$settings["clock/show_seconds", False]

End

Private Sub ShowSeconds_Write(Value As Boolean)

    Me._$settings["clock/show_seconds"] = Value
    Me._$settings.Save()

End
