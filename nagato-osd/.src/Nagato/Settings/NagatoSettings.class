' Gambas class file

Create Static

Public _$settings As New Settings

Event Changed(argKey As String)

Public Sub _ChangeProperty(argKey As String, argValue As Variant)
    
    Me._$settings[argKey] = argValue
    Me._$settings.Save()
    Raise Changed(argKey)
    
End
