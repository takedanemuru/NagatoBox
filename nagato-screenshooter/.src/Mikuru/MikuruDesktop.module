' Gambas module file

Public Function HasActiveWindow() As Boolean
    
    If Not NagatoArguments.OnlyActiveWindow Then Return True
    If Desktop.ActiveWindow > 0 Then Return True
    
    Message.Warning("YUKI.N > I couldn't find active window...")
    
    Return False
    
End
