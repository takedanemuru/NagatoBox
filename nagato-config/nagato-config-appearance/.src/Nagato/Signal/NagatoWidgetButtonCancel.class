' Gambas class file

Inherits NagatoWidget

Private $button As Button

Public Sub _InitializeWidget(argContainer As Container) ' virtual
    
    $button = New Button(argContainer) As "Button"
    
    With $button
        .W = 120
        .Text = ("Cancel")
        .Enabled = True
    End With
    
End