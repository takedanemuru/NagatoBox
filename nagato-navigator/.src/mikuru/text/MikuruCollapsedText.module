' Gambas module file

Public Function Get(argText As String, argLength As Integer) As String
    
    If String.Len(argText) <= argLength Then
        Return argText
    Else
        Return String.Left(argText, argLength) & "..."
    Endif
    
End
