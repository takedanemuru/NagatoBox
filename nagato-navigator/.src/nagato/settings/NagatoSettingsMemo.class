' Gambas class file

Inherits NagatoSettings

Create Static

Property Recent As String[]

Private Function Recent_Read() As String[]

    Return Me._$settings["memo/recent", Null]

End

Private Sub Recent_Write(Value As String[])

    Me._$settings["memo/recent"] = Value
    Me._$settings.Save()
    
End
