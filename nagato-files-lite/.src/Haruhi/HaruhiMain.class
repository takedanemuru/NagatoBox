' Gambas class file

Private $left As NagatoFilerContainer
Private $right As NagatoFilerContainer

Public Sub Form_Open()

    Application.MainWindow = Me
    NagatoSettingsForm.Load(Me)

    $left = New NagatoFilerContainer(HSplit1, User.Home, "LeftPane")
    $right = New NagatoFilerContainer(HSplit1, User.Home, "RightPane")

End

Public Sub Form_Close()
    
    NagatoSettingsForm.Save(Me)
    
    Quit 0 ' to kill file completion
    
End
