' Gambas class file

Property Read All As String[]

Private $titles As New String[]

Public Sub _new()
    
    With $titles
        .Add(("Process ID"))
        .Add(("Status"))
        .Add(("Process Name"))
        .Add(("Processor"))
        .Add(("Memory (RSS)"))
        .Add(("Memory (Virtual)"))
        .Add("")
    End With
    
End

Private Function All_Read() As String[]

    Return $titles

End
