' Gambas class file

Inherits NagatoWidget

Private $drawingArea As DrawingArea
Private $observer As Observer
Private $screen As NagatoPainterScreen
Private $lines As NagatoPainterHistoryLines

Public Sub _OnInitializeWidget()
    
    $drawingArea = argDrawingArea
    $drawingArea.H = 120
    
    $observer = New Observer($drawingArea) As "Asakura"
    $screen = New NagatoPainterScreen(argDrawingArea)
    $lines = New NagatoPainterHistoryLines(argDrawingArea)
    
End

Public Sub Refresh()
    
    $drawingArea.Refresh()
    
End

Public Sub Asakura_Draw()
    
    Paint.Begin($drawingArea)
        $screen.Paint()
        $lines.Paint()
    Paint.End()
    
End
