' Gambas class file

Private $pictureSelector As NagatoPictureSelector

Event Select(argFullPath As String)

Public Sub Form_Open()

    Me.Center()

    $pictureSelector = New NagatoPictureSelector(Me) As "PictureSelector"

End

Public Sub PictureSelector_Select(argFullPath As String)
    
    Raise Select(argFullPath)
    Me.Close(1)
    
End
